/******************************************************************************
 *  % more tiny.txt
 *  S O R T E X A M P L E
 *
 *  % ./@SORTER@ < tiny.txt
 *  A E E L M O P R S T X
 *
 ******************************************************************************/


#include <cassert>
#include <iostream>
#include <string>
#include <vector>

#include "@SORTER@.h"
#include "SortUtils.h"

int main(int argc, char *argv[]) {
    std::vector<std::string> a;
    for (std::string item; std::cin >> item;)
        a.push_back(item);
    algs4::@SORTER@::sort(a);
    assert(algs4::SortUtils::isSorted(a));
    algs4::SortUtils::show(a);
    return 0;
}
